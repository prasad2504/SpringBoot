package com.Assignment.hw.Controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RequestMapping("/demo")
@RestController
public class Democontroller {

	
	
	/**
	 * Pagination,Exception Handling,lazy vs Eager,Actuator, Banner change
	 * ResponseEntity,Status Codes,ManyToMany,
	 *Mail Sender,validations
	 * 
	 * N+1
	 *  Spring Security:RBA,JWT
	 *  
	 *  
	 *   Spring Profiles: dev, test, prod
	 * 
	 * YAML
	 * Microservices communication (sync and async),API gateway,Service Discovery, Oauth,Oauth2,
	 * Junit and mockito=>unit and integration testing.
	 * 
	 */
	
	
	
}
